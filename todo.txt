- Fix mess with calling UI code from scene code (e.g. whenever a spawn event triggers, don't clear game menu, etc...)
- Replace GLFW with SDL
- Refactor renderer (actually refactor EVERYTHING)
- Add audio
- Fix terrain pointer
- Fix frustum culling weirdness
- Fix shadows
- ADD THE FUCKING WEAPONS
- Add trees, etc...
